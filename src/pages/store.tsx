import Head from "next/head";
import React from "react";

const Store = () => {
  return (
    <div>
      <Head>
        <title>Cửa hàng</title>
      </Head>
    </div>
  );
};

export default Store;
